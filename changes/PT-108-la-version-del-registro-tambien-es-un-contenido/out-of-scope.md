# Fuera de alcance — `PT-108`

```
OUT: buscar la version en cualquier JSON del proyecto
     Casaria con dependencias y con datos ajenos. REGISTRY.json tiene su campo declarado.

OUT: pasar esta escritura por el cerrojo de PT-107
     Vive en otra herramienta que no lo exporta. Se DECLARA el limite en vez de fingir que
     esta protegida (RULE-06).

OUT: rehacer como se numera la suite
     Se arregla la LECTURA, como en PT-102.

OUT: reserializar el JSON
     JSON.stringify reordena claves y cambia el sangrado. PT-107 demostro EL MISMO DIA lo que
     cuesta escribir mal ese archivo.
```
