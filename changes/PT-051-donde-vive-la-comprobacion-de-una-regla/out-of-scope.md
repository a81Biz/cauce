# PT-051 — Fuera de alcance   `PHASE 4` · `SUITE-R44`

| Qué queda fuera | Dónde va |
|:---|:---|
| Escribir verificadores para las 62 reglas sin ninguno | — |
| Partir `verify-fdge.mjs` (`TD-02`) | — |
| Abrir el editor en la línea | — |
| Decir **quién llama** a la función que emite el `fail()` | — |
| La entrada de `CHANGELOG` del lote y el número de versión | EP-014 |

La primera lleva `—` porque es deuda **medida** en `TD-08` y el firmante ya decidió acotarla en
`PT-015`. Esta tarea la hace más visible —una a una en vez de como una cifra— sin tocarla.

La cuarta lleva `—` y es una frontera declarada: `--donde` responde **dónde se emite**, no **por**
**qué se llega ahí**. Lo segundo exige analizar el flujo de llamadas, y prometerlo sin hacerlo
sería peor que no ofrecerlo.
