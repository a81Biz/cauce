# PT-092 — Fuera de alcance   `SUITE-R44`

| Qué queda fuera | Por qué | Destino |
|:---|:---|:---|
| **Ejecutar `FIDE`** | Incuba un proyecto desde una idea de negocio y no hay proyecto que incubar. Forzar uno es el proyecto-de-prueba-configurado-para-que-salga-bien que `PT-072` rechazó | `R-001` del roadmap |
| Levantar una interfaz para poder ejecutar `QA` | Construir producto para satisfacer un verificador es la inversión exacta del marco | `—` |
| **Actuar sobre lo que `FPGE` encontró** | `FPGE-R04` prohíbe promover desde el roadmap. Los ocho nacen `DRAFT` y entran por `FDGE PHASE 1` cuando una persona los pase a `READY` | `EP` siguiente |
| Arreglar `INC-001` | Que un cierre de PTSA no pueda desaparecer sin que nada lo note es una comprobación nueva. Aquí se reconstruye lo perdido y se registra | `R-005` del roadmap |
| **Cerrar `H-008`** | `PTSA-R44` reserva el cierre de un `DOMAIN` a una persona. Queda en `VALIDATION_PENDING` con lo que falta escrito | `—` |
| Las 21 celdas `NO_EVALUADA` de la matriz | Cada una tiene motivo y coste en `COVERAGE.md` §3. Evaluarlas es trabajo, no un retoque | `R-008` del roadmap |

## La primera fila es la que deja el lote incompleto, y se dice

`TD-15` pasa de tres componentes sin ejecutar a **uno pendiente y uno que no aplica**. El pendiente
es `FIDE`, y **es el primer candidato del roadmap** con la prioridad más alta de los ocho.

No entra aquí porque necesita algo que no existe: un proyecto que incubar. Inventarlo para cerrar
un número sería exactamente lo que este lote combate.
