# Parada de `PT-178` → abre `PT-181`

**Motivo:** `hallazgo` · **Desenlace:** `abre`

## Qué paró

Una pregunta del firmante durante `PT-178`, registrada en `SESSION_LOG.md`. Preguntó por dos
síntomas —*«muchos PT sin intake»* y *«un error al cazar una cadena, que se suponía resuelto»*— y por
si había algo que **nada obligara a hacer**.

De esa misma pregunta salieron **cuatro** tareas, y el `SESSION_LOG` las enumera juntas:

```
PT-179 · verify-fdge concede sin mirar la fase
PT-180 · el slug del registro y el de la carpeta divergen
PT-181 · la expectativa de un caso se compara como regex
PT-182 · el mapa fase→artefacto esta a mano en dos herramientas
```

`PT-179`, `PT-182` y `PT-183` ya citan `PT-178` como su parada. **`PT-181` no**, y es la única
allocation del repositorio que seguía sin citarla.

## Por qué se registra ahora, y qué no se está haciendo

`FDGE-R55` es **posterior** a esta tarea, así que `PT-181` no incumplió nada al nacer: la deuda es
del registro, no de quien la abrió.

**No se inventa el origen.** Se toma del `SESSION_LOG`, que enumera las cuatro en el mismo párrafo y
por la misma causa, y de que sus tres hermanas ya lo tienen registrado así. Si el `SESSION_LOG` no
lo dijera, se habría dejado como deuda declarada en vez de deducirlo (`RULE-06`).

## El hallazgo

> *la expectativa de un caso se compara **como regex**: 303 de 1476 llevan metacaracteres.
> `SUITE-R59` **no** cubre esto — allí el patrón se rompe, aquí se interpreta.*

Medido de nuevo el `2026-08-28`, sobre el arnés actual: **257 de 1384 (19 %)**.

**Abre `PT-181`** (`EP-026`, `BUG`, severidad pendiente hasta el `2026-09-30`).
