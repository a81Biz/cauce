# Parada de `EP-023` → abre `PT-197`

**Motivo:** `abre-trabajo` · **Desenlace:** `abre`

## Qué paró

El aplazamiento de `EP-023` durante el cierre de `EP-025`. El lote llevaba desde el `2026-08-24`
admitido con `G1 CHALLENGE`, **sin descomponer y con cero tareas**, y sus cuatro filas de cierre en
`PENDIENTE` **bloqueaban el `G4` de `EP-025`**.

## Por qué el trabajo se mueve en vez de morir con el lote

El Dictamen —el séptimo componente, el entregable ejecutivo al dueño del dominio— **sigue siendo
trabajo querido**. Lo que falló no fue la idea: fue admitir un lote que nadie descompuso.

`SUITE-R44` es explícita: aplazar **pone** en el tablero, no saca. Así que `EP-023` se aplaza con
reentrada, fecha y dueño, y su contenido pasa a un identificador que **sí nace descompuesto**.

## Lo que este movimiento reconoce

El `CHALLENGE` del agente tenía razón. `DoR-E6` exige que cada `PT` del lote tenga su intake, y
`EP-023` no tenía ninguno; se admitió igualmente por decisión del firmante, *«para que no se me
olvide»*. **El hueco no desapareció del registro: reapareció cuatro días después, en la compuerta de
otro lote.**

Es la mejor evidencia de para qué sirve `INTAKE-R07`, y por eso queda escrito aquí y no sólo en el
registro.

**Abre `PT-197`** (`EP-026`, `FEATURE`, `S2`).
