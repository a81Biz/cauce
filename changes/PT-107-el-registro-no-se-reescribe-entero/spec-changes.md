# Cambios de especificación — `PT-107`

**Ninguna regla cambia.**

| | |
|:---|:---|
| Reglas nuevas | ninguna |
| Reglas modificadas | ninguna |
| Reglas derogadas | ninguna |
| `RIGE_DESDE` | no aplica |
| Vocabulario nuevo | ninguno |

`SUITE-R08` ya declaraba este archivo **el único asignador de identificadores**, y `LEX-R04` ya
exigía que un identificador sea monotónico y **nunca reutilizado**. Las dos estaban bien; lo que
faltaba era que la herramienta pudiera cumplirlas.

Esta tarea no añade una obligación: **hace exigible una que ya existía**, que es la misma forma
que `PT-103` y `PT-105`.
