# `PT-168` · `out-of-scope.md` — `PHASE 4` · `SUITE-R44`

| Qué queda fuera | Por qué | Dónde va |
|:---|:---|:---|
| Arreglar los huecos que aparezcan | Esta tarea corrige el **instrumento**; lo que mida es otra decisión. No hizo falta: el árbol real sigue sin huecos | — |
| Las otras dimensiones de `audit` —reglas, triggers, artefactos— | Cabe que tengan el mismo patrón. **No está medido**, y suponerlo sería el mismo error | — |
| Reescribir `## FPGE` de `PHASES.md` para que use `PHASE n` | Sería arreglar el documento para que encaje con el lector, en vez de leer lo que hay | — |
