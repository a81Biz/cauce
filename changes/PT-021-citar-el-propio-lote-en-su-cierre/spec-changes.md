# PT-021 — Cambios de especificación   `PHASE 4` · `FDGE-R22`

| Documento | Cambio |
|:---|:---|
| `RULES.md` · `SUITE-R44` | Reescrita **entera**. (a) El propio lote vale en `DONE` o `CLOSED`, no solo `CLOSED`. (b) Se sustituye la descripción de la lista de palabras —que `PT-018` quitó del código y dejó escrita aquí— por la gramática cerrada que el código sí implementa |
| `CORE.md` | Regenerado |

El punto (b) **no lo introduce esta tarea**: lo arrastraba `PT-018`. Se corrige aquí porque
dejar la regla escrita diciendo lo contrario de lo que ejecuta un día más es peor que tocar un
documento fuera del alcance mínimo. Queda anotado en el descubrimiento y en el `CHANGELOG`.
