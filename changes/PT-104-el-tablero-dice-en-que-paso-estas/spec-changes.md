# Cambios de especificación — `PT-104`

**Ninguna regla cambia.** No se crea, deroga ni reescribe ninguna.

| | |
|:---|:---|
| Reglas nuevas | ninguna |
| Reglas modificadas | ninguna |
| Reglas derogadas | ninguna |
| `RIGE_DESDE` | no aplica |
| Vocabulario nuevo | ninguno |

Lo que cambia es **qué publica** el cuerpo derivado de un issue. `SUITE-R35` ya manda sobre eso
—«el registro asigna, la plataforma espeja»— y esta tarea la cumple mejor, no la modifica:
espeja **más estado**, y sigue sin copiar contenido.

`FASES` ya declaraba `nombre`, `produce` y `cierra`; `queSigue` ya derivaba `bloqueos` y
`avisos`. Ninguno de los dos se toca.
