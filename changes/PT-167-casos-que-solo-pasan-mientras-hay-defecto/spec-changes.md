# `PT-167` · `spec-changes.md` — `PHASE 4`

**Ninguna regla nueva.** El barrido se acoge a `SUITE-R61`, que `PT-169` escribió: la misma regla
cubre los tres patrones de caso muerto, y el `invertido` es uno de ellos. Escribir una regla propia
habría partido en dos un hecho que ya tiene dueño (`RULE-01`).

| Documento | Cambio |
|:---|:---|
| — | ninguno |

**Contrato de herramienta**: dos funciones nuevas en `patrones.mjs`. Aditivo.

**No sube versión.**
