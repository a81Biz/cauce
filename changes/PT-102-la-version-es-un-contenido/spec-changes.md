# Cambios de especificación — `PT-102`

**Ninguna regla cambia.** Esta tarea no crea, deroga ni reescribe ninguna.

Lo que cambia es **dónde vive un patrón crítico**, y eso ya lo mandaba `SUITE-R38` («un hecho,
una fuente»). El arreglo no añade una obligación: hace cumplible una que ya existía.

| | |
|:---|:---|
| Reglas nuevas | ninguna |
| Reglas modificadas | ninguna |
| Reglas derogadas | ninguna |
| `RIGE_DESDE` | no aplica |
| `CHANGELOG` | entrada en el sello del lote (`SUITE-R57`), no aquí |

**Documentos tocados por el realineado**, que es el efecto de la tarea y no un cambio de
especificación: `MANUAL.md`, `README.md`, `Suite-CLAUDE-Template.md` y el `CLAUDE.md` del
repositorio pasan de declarar `7.4.0`, `5.2.0`, `5.2.0` y `10.0.0` a declarar `11.0.0`.
