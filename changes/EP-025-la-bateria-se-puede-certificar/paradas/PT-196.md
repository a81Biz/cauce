# Parada de `EP-025` → abre `PT-196`

**Motivo:** `compuerta` · **Desenlace:** `abre`

## Qué paró

`verify-fdge --gate G4`, cerrando `EP-025`, sobre **una fila que no se puede resolver**:

```
✗ SUITE-R45  EP-025: 1 fila(s) sin resolver en G4: «El tag y la publicación».
             Cada una declara HECHO o el identificador al que se movió.
```

## La contradicción, en dos reglas del mismo marco

| Regla | Qué dice |
|:---|:---|
| `SUITE-R45` | La fila se resuelve **en** `G4` |
| `SUITE-R06a` | El tag va **después** del merge |

`G4` **es** el merge. En `G4` el tag no puede estar `HECHO` **ni por definición**, y la regla exige
que lo esté. No es una fila mal escrita: es que **no hay respuesta correcta**.

## Lo que hay debajo, y es más grande

`PHASE 9` termina en el merge. Todo lo que viene después **no pertenece a ninguna fase**:

```
integrar → cerrar → cierre del lote → proyectar → tag → borrar ramas
```

Está descrito en `PHASES.md` como una lista de comandos, sin fase que lo posea, sin artefacto que lo
cierre y sin compuerta que lo mire. Por eso `SUITE-R45` pide en `G4` una respuesta que sólo existe
después.

## Y su consecuencia medida: el ciclo de dos viajes

Cerrando este mismo lote:

```
tracker integrar  → escribe INTEGRATED en la rama de trabajo
tracker cerrar    → EXIGE que INTEGRATED este YA en main   (SUITE-R46)
```

**Cerrar un lote pasa por `G4` dos veces.** El `HANDOFF` lo tenía anotado como hallazgo suelto —«el
ciclo de dos viajes al tablero»— **sin ninguna tarea que lo reclamara**, desde `PT-186`.

## Lo que lo hace tarea y no nota

Lo dijo el firmante con la causa, no con el síntoma:

> *«no está contemplado dentro del marco la forma de "terminar" la épica. Es decir, se termina hasta
> que está en main con todo lo necesario»*

**Abre `PT-196`** (`EP-026`, `BUG`, `S2`).
