# Cambios de especificación — `PT-112`

**Ninguna regla cambia.**

`SUITE-R06e` ya decía que modificar `docs/methodology/` no se automatiza. `SUITE-R31` ya decía que
decidir la divergencia es humano. `EXEC-R04a` ya daba la forma de una constancia.

Las tres estaban bien. Lo que faltaba era que **la herramienta no permitiera saltárselas sin
rastro**.
