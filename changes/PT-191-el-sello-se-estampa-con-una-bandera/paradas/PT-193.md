# Parada de `PT-191` → abre `PT-193`

**Motivo:** `compuerta` · **Desenlace:** `abre`

## Qué paró

El `npm run verify` de cierre de `PT-191`. `verify:secretos` bloqueó:

```
FND-R29  1 hallazgo(s)
  contraseña en texto plano · historia fb10d3de:1
    echo "password = REDACTADO"; } > "$d/a.sh"
```

## Por qué

Es el fixture `_sec190` que `PT-190` añadió, con una contraseña **sintética** escrita a propósito
para probar que el escáner la caza. `PT-190` declaró el archivo como señuelo con `cauce:senuelos`,
y eso funciona **en el árbol**.

Pero al **commitear** `fb10d3de`, la línea entró en la historia, y el escaneo de historia mira los
**hunks añadidos** — donde esa exención no llega. El hallazgo no existía hasta que el propio lote
hizo su commit.

## Qué se hizo, y qué falta

**Firmado ya:** la huella `397f02076a3e` en `SECRETOS-EXCEPCIONES.md`. El commit es inmutable, así
que firmarla hacía falta en cualquier caso. Firmar no silencia: sigue apareciendo en cada revisión.

**Falta la causa**, y es lo que abre esta tarea: el **fuente** no debería contener el literal. Hay
precedente exacto — `PT-015` partió en dos mitades la clave de ejemplo de AWS (`selftest.sh:821`)
por este mismo motivo, y dejó escrito el porqué en `:817`.

**Abre `PT-193`** (`EP-025`, `CHORE`, `S3`). Se atiende **dentro de este lote** porque el bloqueo
es de su propia compuerta de cierre.

## El coste, declarado antes de decidir

Tocar `selftest.sh` reabre el bloque 8, así que obliga a otra corrida **completa** (~28 min) y a
volver a sellar antes del PR. Se ofreció al firmante la alternativa —firmar y dejar la causa como
tarea aplazada, sin coste— y eligió corregirla ahora con el coste por delante.

> **Redactado** (`FDGE-R45`): el valor de la contraseña sintética se sustituye por
> `REDACTADO`. Citarlo aquí lo devolvería al repositorio y a la historia — que es
> exactamente lo que esta tarea quita. Lo cazó `FDGE-R45` en el `verify` de cierre.
