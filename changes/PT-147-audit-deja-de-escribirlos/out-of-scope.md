# PT-147 · `out-of-scope.md`

```
OUT: corregir lo que la auditoria de FIDE o FPGE destape. Que APAREZCAN es el objetivo;
     que salgan limpios no lo es. Si salen rojos, se declara y, si merece trabajo, entra
     como tarea propia — enlazada, no como nota.

OUT: escribir el apartado de FPGE en LEXICON §3. Es PT-f de EP-024. Hacerlo desde una
     herramienta es la direccion que LEX-R21 prohibe: LEXICON manda sobre el dato.

OUT: cambiar el mecanismo de «cubre». Reconoce tres formas de declarar una fase —PHASE n
     suelta, rango PHASE a-b, y linea compacta de componente— y funciona. RC-02 lo fija.

OUT: unificar las claves de audit entre nombre y sigla. El contrato separa los dos y cada
     consumidor pide el que necesita; cambiar las claves seria un cambio de comportamiento
     y esta tarea declara tener solo uno.

OUT: PT-148 y PT-149.
```
