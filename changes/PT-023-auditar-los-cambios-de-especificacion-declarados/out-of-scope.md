# PT-023 — Fuera de alcance   `PHASE 4` · `SUITE-R44`

| Qué queda fuera | Dónde va |
|:---|:---|
| Un verificador de `FDGE-R22` que compruebe que la declaración se cumplió | — |
| Reescribir `SUITE-R44` o `FDGE-R22` | — |
| Los choques entre reglas encontrados de paso | PT-029 |
| La entrada de `CHANGELOG` del lote y el número de versión | EP-013 |

La primera fila lleva `—` a propósito, y es el resultado de la tarea, no una omisión: la medida
de `PHASE 2` dice que ese verificador **no debe existir** —75 % de falsos positivos—, así que no
se aplaza a nadie. Queda fuera y punto.

`PT-029` es hermano de este lote: la cita es recíproca por serlo (`SUITE-R44`).
