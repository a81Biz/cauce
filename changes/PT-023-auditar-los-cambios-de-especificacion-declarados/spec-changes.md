# PT-023 — Cambios de especificación   `PHASE 4` · `FDGE-R22`

| Documento | Cambio |
|:---|:---|
| `FDGE-Prompts.md` | El párrafo de `SUITE-R44` pasa de describir la intención a enunciar la forma: vocabulario cerrado —`—` o identificador, nada más— y los tres casos de reciprocidad. Desaparece «normalmente» |

**Ninguna regla cambia.** `SUITE-R44` ya dice todo esto desde `PT-018`; lo que cambia es que el
texto **copiable** deje de decir menos que la regla que cita (`SUITE-R20`, `LEX-R22`).

**Sin cambio de versión.** Es la corrección de una declaración de `PT-018` que quedó sin ejecutar,
dentro de un lote que ya sube `MAJOR` por `PT-016`.

> Esta tabla tiene una fila y **una sola**, a propósito. `PT-018` puso tres y ejecutó una; lo que
> esta tarea aprendió es que nada comprueba la diferencia, así que declarar de más no es
> ambición: es una promesa que nadie va a cobrar.
