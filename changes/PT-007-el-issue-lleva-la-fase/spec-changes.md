# PT-007 — Cambios de especificación   `PHASE 4`

## Reglas

**Ninguna.** `SUITE-R35` ya dice que la plataforma espeja; esto amplía **qué** espeja. `CORE.md`
no se regenera.

## Contrato de `tracker`

| | Antes | Después |
|:---|:---|:---|
| Etiquetas de un issue | `tarea` \| `implementación` | y además `fase: N` y `G1`..`G4` |
| `espejo` | dos direcciones | y además las etiquetas contra el registro |
| Acción `estado` | — | tablero desde el registro, sin tocar la plataforma |

## Datos

Ninguno. `phase` ya existe desde `PT-004`; la compuerta se deriva.
