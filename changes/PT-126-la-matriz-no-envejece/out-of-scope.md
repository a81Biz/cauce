# Fuera de alcance — `PT-126`

> `SUITE-R44` · La última columna es el destino, y es vocabulario cerrado.

| Qué queda fuera | Por qué | Dónde va |
|:---|:---|:---|
| Priorizar los candidatos | La fórmula de `FPGE-R06` no cambia | — |
| Promover nada | `FPGE-R04` lo prohíbe y sigue prohibiéndolo: el marco propone, la persona dispone | — |
| Dar dueño a las seis clases huérfanas | Cada una es una decisión de diseño propia. Aquí se **publican** como candidatos, que es lo que el intake pidió | `FPGE` · decide el firmante |
| Que `SUITE-R59` pueda fallar | Es el hallazgo de `PT-119`. Escribir esa comprobación es una tarea con su propio alcance, y el aviso ya la nombra en cada `sellar` | `FPGE` · candidato publicado |
| Que `sellar` lea `EVENTOS.jsonl` en vez de `MATRIZ.md` | Sería una segunda derivación del mismo hecho, y podría dar otro número (`CE-008`). La frescura de la matriz la comprueba `matriz:check` | — |
| Hacer obligatorio el campo `Clase de evento` | Exigirlo siempre haría que se inventara una clase para callar el aviso, y eso es peor que no tener aviso | — |
| Retrofechar el aviso a las entradas anteriores | `SUITE-R09`. `RIGE_DESDE` acota `LEX-R31` a la `13.0.0` | — |

---

## Lo que esta tarea **produce** y no resuelve

**Seis candidatos y un séptimo caso peor**, publicados en cada `sellar` sin que nadie los
transcriba:

```
CE-004  8x  Probar donde trabajo, no donde se decide
CE-001  7x  El proxy en lugar del hecho
CE-003  7x  Un argumento se cuela por la detección de ROOT
CE-005  5x  Verde por no haber mirado
CE-015  4x  El cierre destapa más que el reparto
CE-007  3x  Existe la herramienta y nada la echa en falta
CE-002      tiene regla (SUITE-R59) y NADA EMITE POR ELLA
```

Ninguno lo eligió nadie: salen de la evidencia. Decidir cuáles se convierten en trabajo es del
firmante.
