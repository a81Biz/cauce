# PT-029 — Cambios de especificación   `PHASE 4` · `FDGE-R22`

| Documento | Cambio |
|:---|:---|
| — | ninguno |

**Ninguna regla cambia, y ningún documento de la metodología se toca.** `FDGE-R23`, `FDGE-R25` y
`FDGE-R29` dicen lo correcto: lo que estaba mal era **cuándo** se comprobaban, y eso vive en
`tools/`, no en `RULES.md`.

`PT-023` acaba de medir que nada comprueba que una declaración de esta tabla se haya cumplido.
Declarar un cambio que no hace falta sería una promesa que nadie va a cobrar; declarar «ninguno»
cuando es ninguno es la única forma de que la tabla siga valiendo algo.
