# PT-074 — Cambios de especificación   `PHASE 4`

`FDGE-R22`.

| Qué | Antes | Después |
|:---|:---|:---|
| Cuerpo del issue | tipo · severidad · lote · enlace | **y el veredicto de viabilidad** con su naturaleza y su `medido_en` |
| `MARGINAL` / `UNSAFE` | sólo en `LEXICON` §6.5d | además, su consecuencia dicha en el issue |
| Los quince veredictos de `EP-017` | `medido_en: 258be16` (sesión huérfana) | re-registrados contra la marca correcta |

**Ninguna regla nueva.** `FDGE-R54` ya existe (`PT-075`) y esto es `SUITE-R35` aplicada a un
campo que se quedó fuera del espejo.

**No se copia contenido al issue** (`SUITE-R35`): sólo el veredicto y su base. El razonamiento
sigue en `changes/PT-NNN-slug/`.

`CHANGELOG`: `PATCH`. La versión la fija `EP-017`.
