# PT-042 — Diseño   `PHASE 4` · `FDGE-R21`

```js
const man = join(SUITE_EN_DESTINO, 'MANUAL.md');
if (existsSync(man)) { 1 · MANUAL · 2 · CASOS-DE-USO · 3 · CORE }
else                 { lo dice, y sigue: CORE.md es lo único obligatorio }
```

Y en `install`, **antes** de «abre tu agente»: los dos documentos, en orden, con una línea que
explica por qué — «instalar copia archivos; leerlos es lo que hace que sirvan».

`SUITE-R54` declara las dos cosas: lo que hace **y lo que no puede hacer**.
