# PT-042 — Estrategia   `PHASE 3`

| Camino | Por qué |
|:---|:---|
| Exigir confirmación de lectura | El agente afirmando sobre sí mismo (`SUITE-R27`). No prueba nada |
| Volcar el manual en `CORE.md` | Coste en cada sesión y una copia que diverge (`SUITE-R15`) |
| **Ponerlo delante y hacerlo consultable** | Es lo que se puede, y se declara que es lo que se puede |

```
cauce install   EMPIEZA remitiendo al manual, antes de «abre tu agente»
cauce start     1 · MANUAL   2 · CASOS-DE-USO   3 · CORE
```

El orden no es cortesía: **conocer 179 reglas no es lo mismo que saber usarlas**.

## La autorreferencia ya existía repartida

`cauce regla <ID>` para una regla, `tracker siguiente` para la fase, `CASOS-DE-USO.md` para el
caso. Faltaba que el arranque las nombrara.

## Si el manual no está

Se **dice**, y el marco sigue: `CORE.md` es lo único obligatorio. Fingir que está sería peor que
su ausencia (`RULE-06`).
