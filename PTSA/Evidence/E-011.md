---
id: E-011
origen: npm pack --dry-run --json  +  grep sobre CLAUDE.md, despues de corregir H-001 y H-006
lineas: —
capturada: 2026-08-20
auditoria: PTSA-2026-08-20
fingerprint: bcb35ab1d891
---

# E-011

> **Origen** · `npm pack --dry-run --json  +  grep sobre CLAUDE.md, despues de corregir H-001 y H-006`
> **Capturada** · 2026-08-20 · **fingerprint** `bcb35ab1d891`
> Evidencia **post-correccion** observada en la fuente real (`PTSA-R39`), no inferida del diff.

## Contenido capturado

```
$ node -e "...package.json files..."
antes:   ["bin","docs/methodology","!docs/methodology/.claude","README.md","LICENSE","NOTICE"]
despues: ["bin","!bin/graphify-out","docs/methodology","!docs/methodology/.claude","README.md","LICENSE","NOTICE"]

$ rm -rf bin/graphify-out
$ npm pack --dry-run --json
archivos: 57
   17664 bin/cauce.mjs

PRUEBA DE PERMANENCIA — se recrea el artefacto y se vuelve a empaquetar:
$ mkdir -p bin/graphify-out/cache/ast
$ echo '{"nodes":[{"source_file":"C:\\ruta\\local"}]}' > bin/graphify-out/cache/ast/prueba.json
$ npm pack --dry-run --json
archivos: 57 · artefactos de graphify empaquetados: 0
$ rm -rf bin/graphify-out
$ ls bin/
cauce.mjs

$ grep -n "HERRAMIENTAS ───" CLAUDE.md
59:|   -- HERRAMIENTAS --- 16, y ninguna es opcional ---
$ ls docs/methodology/tools/ | wc -l
16
$ grep -n "El binario" CLAUDE.md
29:bin/cauce.mjs   El binario: install . verify . compare . core . start . regla . version
$ grep -oE "^  [a-z]+\(\)" bin/cauce.mjs
install verify compare core start regla version
```

## Observacion factual

El tarball pasa de 58 a 57 archivos y no contiene ningun artefacto de graphify, ni siquiera
cuando el artefacto existe en el arbol. Las dos cifras de CLAUDE.md coinciden ahora con `ls` y
con la enumeracion de comandos del binario.

> Sin conclusiones de causa: las causas viven en el hallazgo (`PTSA-R32`).
