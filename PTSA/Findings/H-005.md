---
id: H-005
tipo: BUG
dimension: D2
severidad: MEDIA
impacto: Medio
probabilidad: Probable
estado: VALIDATION_PENDING
abierto: 2026-08-20
auditoria: PTSA-2026-08-20
evidencias: [E-006]
---

# H-005 — El grafo esta en .gitignore, asi que FDGE-R43 solo puede dar FRESH en la maquina que lo genero

> `D2` · **MEDIA** · tipo `BUG` · estado `VALIDATION_PENDING`
> Evidencia: [[E-006]]

## Qué se observó

Al regenerar el grafo durante el sello, `git status` no mostró un solo archivo:

```
git check-ignore -v graphify-out/graph.json
.gitignore:13:graphify-out/     graphify-out/graph.json
```

`verify-fdge.mjs:267` devuelve `MISSING` si el directorio no existe, y `verify-fdge.mjs:1347` hace
que todo lo que no sea `FRESH` ni `SUSPECT` **bloquee `G2` en un PT `MAJOR`**. En un clon limpio
—CI incluida— el directorio nunca existe.

Y la deriva de contenido se calcula contra **`mtime` locales**, que `git clone` reescribe: aunque
el grafo viajara, el cálculo daría otra cosa en cada máquina.

## Consecuencia medida

El sello de la `10.0.0` se resolvió con un grafo verificado `FRESH` **en una sola máquina**, y esa
comprobación no la repite nadie. Es el mismo patrón de `H-003` aplicado al **alcance** de la
comprobación en vez de a su contenido.

## Qué lo corrige

Tres salidas, y elegir es diseño sobre `docs/methodology/` (`SUITE-R06(e)`):

1. Versionar `graphify-out/` — mete ~1 MB de artefacto generado en la historia.
2. Sustituir `mtime` por hash de contenido — hace el cálculo portable, no el archivo.
3. Convertir `MISSING` en un aviso explícito de «no evaluable en este clon» en vez de un bloqueo
   que nunca se dispara porque nadie llega a `G2` en CI.

Declarado en `TD-17`.

## Revision — 2026-08-20 · corregido, y el analisis del hallazgo era incompleto

**Estado: `VALIDATION_PENDING`.** `PTSA-R44` reserva el cierre de un `BUG` a una persona.

### Habia DOS defectos, no uno

Este hallazgo describia que `graphify-out/` esta en `.gitignore`. Al medirlo aparecio que **el
manifiesto guarda `ast_hash` en la misma linea que `mtime`**, y `derivaDelGrafo` usaba el `mtime`.
No faltaba el dato: se eligio el barato teniendolo al lado.

Y **las rutas del manifiesto son absolutas**. Eso **invalida** lo que este hallazgo daba por
hecho: versionar el grafo NO habria resuelto el problema, porque el manifiesto solo sirve en un
disco donde el proyecto este exactamente en esa ruta.

### Lo que se hizo, y lo que se descarto con su cifra

```
versionar graphify-out/   2,3 MB medidos, medio graph.html regenerable, conflicto por
                          regeneracion — DESCARTADA, y SUITE-R37 ya lo declara regenerable
mtime -> hash             ADOPTADA · casi cero, el hash ya estaba
MISSING explicito         ADOPTADA · una linea
```

### Evidencia post-correccion, observada   `PTSA-R39`

```
antes   SUSPECT · 6 de 17 — incluyendo archivos NO tocados, por normalizacion CRLF
ahora   SUSPECT · 5 de 17 — docs/methodology/tools/patrones.mjs, …/selftest.sh,
                            …/tracker.mjs, …/verify-fdge.mjs, …/verify-suite.mjs
```

**Exactamente los cinco editados en el lote.** Menos ruido y mas precision a la vez.

### Lo que sigue abierto, y por eso no se cierra del todo

**En un clon limpio el grafo sigue sin existir.** `MISSING` pasa de un bloqueo que nadie
alcanzaba a un «no evaluable aqui» — honesto, y no es lo mismo que comprobarlo. `TD-17` se
actualiza al cerrar `EP-018` con la decision tomada.
