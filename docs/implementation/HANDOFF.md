# HANDOFF — estado retomable

<!-- ESTADO -->
implementación: ninguna abierta. EP-018 (#162) CLOSED, 11.0.0 sellada y con tag v11.0.0 en main. PT-094 es una tarea SUELTA fuera de lote: un BUG que salio de CI, no de un intake planificado.
tarea:          PT-095 (#181) en DONE con G3 firmada. PR #183 abierto a main con CI en verde. Corrige el EXEC-R04a que juzgaba entradas del 13 de agosto con una regla nacida el 20, en un ledger append-only: no era exigente, era imposible de cumplir.
compuerta:      G3 de PT-095 FIRMADA el 2026-08-21 por Alberto Martinez. G4 AUTORIZADA en el mismo acto: «realiza ya lo necesario a mi nombre, ya debe estar todo listo para publicar. Tienes mi Vo Bo». TRES excepciones declaradas —SUITE-R06b, SUITE-R06a y SUITE-R06b— con constancia. PUBLICAR NO ENTRA: la instruccion dice «listo PARA publicar», y la eleccion registrada del firmante sigue siendo suya.
siguiente:      1) G3 de PT-095: validarlo y firmarlo en HISTORY.log con «G3 YYYY-MM-DD nombre» (FDGE-R26). 2) G4: fusionar el PR #183 — devuelve main al verde Y lleva el INTEGRATED de PT-094, que SUITE-R46 exige en main antes de cerrar su issue. 3) «tracker cerrar --aplicar» cierra el issue #175 de PT-094. 4) «tracker avanzar PT-095 --a 10» cierra el suyo. 5) PUBLICAR: «gh workflow run publicar.yml --ref main -f confirmacion=PUBLICAR». 6) Los siete hallazgos PTSA en VALIDATION_PENDING (PTSA-R44). 7) PT-025 Azure, DEFERRED, issue #35. 8) La calculadora: PT-001 y PT-002 en trabajo con G4 propia, y tres hallazgos devueltos (INC-001..INC-003).
decisiones:     EP-017 acordada el 2026-08-18 con el firmante, que pidio ANTES DE PUBLICAR «una prueba grande contundente» de que la 9.0.0 sirve para proyectos nuevos y legados, y asegurar que MANUAL, README y CASOS-DE-USO estan completos. Dos decisiones suyas en esa conversacion: (a) el legado se prueba con LOS DOS —un proyecto sintetico creado para provocar los casos que Foundation debe detectar, y el proyecto real de Mercados Energeticos, que hasta ahora solo admitia «migrate SIN --apply»: esto AMPLIA esa autorizacion a usarlo como caso de prueba, y la prueba debe seguir siendo NO DESTRUCTIVA, sobre copia o sin --apply—; y (b) cortar la sesion aqui para que la siguiente retome en frio · PUBLICAR LA 9.0.0 NO ESTA AUTORIZADO todavia: el firmante lo condiciona a que el lote cierre. La autorizacion de publicar del 2026-08-18 se uso en la 8.2.0 y no se extiende · G4 de EP-016 y apertura del lote autorizadas el 2026-08-18 por Alberto Martinez: «tienes mi VoBo para terminar y publicar correctamente lo necesario, cerrar completamente la epica y comenzar con la EP-016 sin parar hasta terminar». Publicar se ejecuto para la 8.2.0; la 9.0.0 queda SIN publicar · G4 de EP-015 autorizada el 2026-08-18 por Alberto Martinez: «tienes mi VoBo para el G4 sigue con los PT pendientes hasta terminar la epica. No pares hasta terminar». Publicar NO entra: no se pidio y sigue fuera · G4 de EP-014 resuelta el 2026-08-18 por Alberto Martinez: «haz el PR y el merge con lo que falte de G4 para las tareas y lo que sea bloqueante a mi nombre para que avanzar y continuas con la EP». SIN excepciones declaradas: el unico bloqueo era SUITE-R42, que el propio PR resuelve · G4 de EP-013 resuelta el 2026-08-15 por Alberto Martinez sobre el PR #93: «Cierra primero G4 de EP-013 (…) No modifiques el alcance de EP-014 ni adelantes tareas del lote». En el mismo acto autorizo la EXCEPCION: --gate G4 bloqueaba por las filas de cierre de EP-014 —no de EP-013, que estaba verde—, porque checkEpics recorre TODOS los lotes y enG4 es global. Se integro con ese rojo DECLARADO y el defecto quedo abierto como PT-055 DEFERRED, no arreglado: arreglarlo habria expandido EP-014. Mismo patron que la G4 de EP-011 con PT-046 · las CINCO decisiones del firmante del 2026-08-15, que gobiernan EP-014, EP-015 y EP-016: (1) cauce/<usuario> es DERIVADA, no autorada — no se crea una segunda fuente y el commit atomico queda intacto; (2) los IDs multiusuario se reparten por RANGOS RESERVADOS, sin tocar SUITE-R08 ni namespacear el identificador; (3) el usuario vive en la RAMA DE TAREA y «trabajo» sigue siendo unica, para no anadir un cuarto nivel ni multiplicar G4 contra EXEC-R03; (4) el presupuesto de sesion se DERIVA de senales observables y del historial del repositorio, NUNCA de una cifra de tokens que el marco no puede medir, y distingue MEDIDO / ESTIMADO / SIN EVALUAR; (5) TRES lotes y en este orden, EP-014 fontaneria → EP-015 continuidad → EP-016 topologia, porque el ultimo es el unico que rompe compatibilidad · topologia de ramas confirmada por Alberto Martinez el 2026-08-14 —«si, es correcto»— tras preguntarle: rama de PT a «trabajo» y G4 sigue siendo el merge del LOTE a «main». La alternativa habria convertido G4 en una compuerta por tarea, ocho en EP-013, contra EXEC-R03 ·     G4 de EP-011 resuelta el 2026-08-14 por Alberto Martinez: «Firma a mi nombre y cierra el trabajo, realiza el merge correctamente y verifica que la epica en la que trabajamos este correctamente cerrada» · en el mismo acto autorizo integrar PT-039..PT-042 con su FDGE-R34 en rojo, declarado y abierto como PT-046 — no se toco ninguna entrada de HISTORY · el proyecto legado «Inteligencia de Mercados Energeticos Mexicanos» es el CASO DE PRUEBA del marco, no un proyecto donde trabajar: su migracion se cierra cuando el firmante vaya a trabajar ahi, y hasta entonces solo se ejecuta migrate SIN --apply · 6.0.1 PUBLICADA en npm el 2026-08-13 y verificada desde un directorio limpio: el paquete trae SUITE-R42, SUITE-R43 y las funciones de tracker · en ESTE repositorio no hay paquete que instalar: es cauce, y SUITE-R41 lo prohíbe explícitamente · 6.0.1 decidida el 2026-08-13, PATCH: dos correcciones y ninguna regla nueva ni modificada · la delegación se AMPLIÓ a G3 el 2026-08-13 por decisión del firmante, con constancia en la Revisión 1 del intake de EP-003: SUITE-R06b lo pone en la lista de lo que ningún MODO automatiza, y esto es una persona autorizando una excepción con registro, que es lo que la regla de cumplimiento admite · G4 y la publicación siguen sin delegar · 6.0.0 decidida el 2026-08-13, MAJOR porque SUITE-R42 y SUITE-R43 son reglas vinculantes nuevas — el criterio con el que la 5.0.0 subió · la 5.3.0 nunca se publicó y su contenido va dentro de la 6.0.0 · la 6.0.0 nunca se publicó: la sustituyó la 6.0.1, que corrige los dos defectos que habrían llegado a npm con ella · EP-001 y EP-002 CLOSED, en main desde 9ecb1d3 (PR #7), con GitGuardian resuelto como falso positivo · el registro ASIGNA y GitHub ESPEJA: todo el estado publicado se deriva de REGISTRY.json y ninguna lectura de la plataforma lo alimenta · G1 y G2 se firman por delegación con constancia; G3 de un BUG, G4 y la publicación NO se delegan · los .md se quedan: verify-fdge lee archivos, un issue no está versionado y declarar plataforma es opcional · TD-05 resuelta en la entrada de 5.3.0 · G4 AUTORIZADA AL AGENTE el 2026-08-19 por Alberto Martinez: «realiza el g4 necesario y realiza los merge y pull». Es EXCEPCION DECLARADA a EXEC-R04 y SUITE-R06a, con su registro en SESSION_LOG.md — la via que deja la ultima linea de CLAUDE.md. NO cubre publicar: sigue vigente «No publiques la 9.0.0» · el firmante dudo de que esto sea la 9.0.0 y tiene razon con causa mecanica: verify-fdge usa UNA ancla DESDE=[5,1,0] para cinco comprobaciones de reglas nacidas en versiones distintas, y la guia de migracion de 9.0.0 —que es EP-016— dice «ningun proyecto instalado tiene que hacer nada», falso desde que EP-017 metio FDGE-R54 y SUITE-R56. EP-017 es la 10.0.0 (PT-081) · EP-017 es la 10.0.0 y NO la 9.0.0 (PT-081): la entrada 9.0.0 es el registro fechado de EP-016 y queda INTACTA —cero lineas eliminadas desde v9.0.0—; doblar EP-017 ahi habria borrado que lote trajo que · el tag v10.0.0 NO se ha creado: es lo que fija la linea base del detector de AC-08 y va con publicar, que sigue sin autorizar · «trabajo» PROTEGIDA desde PT-082 con el mismo contrato que main: required_status_checks [marco], strict, enforce_admins. Los commits directos a trabajo ya NO son posibles y hay que abrir PR · PTSA-2026-08-20 es la PRIMERA ejecucion de PTSA sobre este repositorio, pedida por el firmante ANTES de publicar: «no tenemos el marco en un % aceptable… auditar el sistema completamente para buscar, encontrar y eliminar cualquier riesgo». Resultado: certificacion B, Health 79.9, Risk 73, Confidence 0.94, coverage 0.89, nueve hallazgos. Dos corregidos dentro de la auditoria y en VALIDATION_PENDING · el dato que decide el alcance de EP-018: el Acid Test de P-003 PASA — las 20 reglas marcadas CHECK tienen verificador, cero excepciones. Las 101 sin verificador NO incumplen nada (SUITE-R26 aspira, no exige). El hallazgo es que la RUBRICA esta por debajo de la promesa del dominio, no que falte cobertura: por eso EP-018 entra a tres reglas concretas y NO a subir 112/224 · G4 se RETUVO a proposito el 2026-08-20 hasta tener la auditoria: fusionar antes habria sellado una version que el firmante acababa de declarar no lista, con la auditoria llegando despues · VoBo del 2026-08-20 para cerrar pendientes y ejecutar EP-018 entera sin parar: «tienes mi VoBo para hacer todo lo necesario y terminar con los pendientes, comenzar la epica y no parar hasta terminar». Publicar NO entra
no hacer:       confiar en que el handoff DERIVADO diga que sigue cuando no hay tarea en curso: lo probo el cierre de esta sesion y no lo dice —deriva el «sigue» del CHECKPOINT, y con la ultima tarea cerrada no hay nada que derivar—. Lo que sigue esta en ESTE bloque, y EP-017 tiene que recoger ese hueco de PT-060 · publicar la 9.0.0 antes de que EP-017 cierre: es condicion explicita del firmante · escribir MANUAL, README o CASOS-DE-USO antes de ejecutar las dos pruebas: escritos antes describen lo que uno cree que pasa; escritos despues, lo que paso. Cada hueco que aparezca ejecutando es una linea que faltaba · tocar el proyecto de Mercados Energeticos de forma destructiva: la autorizacion es usarlo como CASO DE PRUEBA, sobre copia o sin --apply · probar contra el paquete PUBLICADO: se prueba la 9.0.0 con «npm pack» desde este repositorio, ANTES de publicarla — si la prueba obliga a corregir el marco, se corrige y se vuelve a probar · guardar varias sesiones en UN archivo: dos personas escribirian el mismo y el conflicto volveria. Un archivo POR PERSONA lo evita por construccion (LEXICON 6.5e) · contar una marca sin persona como sesion AJENA: es la de un proyecto de una sola persona, y aparecería como una sesion fantasma · escribir un caso del arnes que dependa de «git config user.name» de la maquina: en CI es el del runner. Paso con «--mio» y es la OCTAVA vez del patron «probar donde trabajo, no donde se decide» · atar una asercion del arnes a una cifra que CRECE —«1[0-9] tareas cerradas»—: fallara algun dia sin que eso signifique nada. Paso al llegar a 20 · separar campos de «git log» con un espacio cuando uno de ellos es un NOMBRE: «Alberto Martinez» se parte en dos. PT-057 pudo porque el SHA no lleva espacios · hacer que verify-fdge FALLE si una rama no lleva usuario: las ramas creadas antes de 8.3.0 son de dos niveles y fallarian todas. AVISA, y el aviso dice desde cuando aplica (FDGE-R19) · escribir una comprobacion que cambie de severidad con el tiempo —«avisa hoy, falla desde la proxima version»—: nadie puede razonar sobre ella, porque el codigo es el mismo y el resultado no · poner dos rangos que se TOQUEN por un extremo: [1,100] y [100,200] comparten el 100, que es exactamente el numero que las dos personas pediran a la vez. Tocarse YA es solaparse · escribir un RegExp con `new RegExp(`...(\d+)...`)` en una plantilla: necesita DOBLE barra o busca la letra «d» y no casa nada. Paso dos veces en PT-062 y se vio ejecutando, no leyendo · agrupar autores de git por PARECIDO —mismo apellido, mismo dominio de correo—: convierte una duda en un dato, y las cuatro tareas siguientes de EP-016 construirian sobre el sin que sus casos lo notaran. «tracker personas» ENCUENTRA los no declarados y PROPONE donde mirar; quien es quien lo dice una persona (LEXICON 6.5f) · hacer que verify-suite exija que toda persona sea firmante: la comprobacion va en UNA direccion —firmante ⇒ persona— y la asimetria es lo que impide que «firmantes:» y «personas» sean copias del mismo hecho · publicar sin comprobar que «publicar.yml» y «verificacion.yml» corren sobre el MISMO repositorio: el primero clonaba en superficial y los casos que derivan del historial fallaban solo ahi. El verde de un workflow no dice nada de otro con distinto checkout · tratar «un dia» como «una sesion»: coinciden POR CASUALIDAD cuando la sesion empezo ese dia, y el dia que no coincidan nada lo notaria. Sin SESSION.json la cifra de sesion es SIN EVALUAR y se DICE (LEXICON 6.5e) · meter CHECKPOINTING, HANDOFF_REQUIRED o WAITING_NEW_SESSION en REGISTRY.json: son de SESION, no de tarea —durante un handoff la tarea sigue IN_PROGRESS— y SUITE-R09 los haria permanentes · derivar la prosa de HANDOFF.md: «decisiones» y «no hacer» son lo UNICO del estado que no se puede derivar, y son lo mas valioso que tiene. «sesion cerrar» produce un handoff que se SUMA, no que sustituye · hacer que la compuerta de viabilidad PROHIBA cuando falta un dato: suena prudente y bloquearia TODO para siempre, porque el disponible es SIN EVALUAR siempre. Se apagaria, y entonces no protegeria el dia que tiene razon. MARGINAL es la respuesta honesta (LEXICON 6.5d) · mover la comprobacion de AC-06 detras del SIN EVALUAR en viabilidadDe: una tarea que no cabe en NINGUNA sesion saldria MARGINAL por faltar el precedente y el bucle infinito volveria. Hay un caso que vigila ese orden y solo ese orden · tratar SIN EVALUAR como cero en cualquier operacion: la inversa de PT-058 lo midio — «restar(100 MEDIDO, SIN EVALUAR)» devolvia 100 ETIQUETADO COMO MEDIDO, o sea que el presupuesto diria que queda todo con autoridad de dato medido · construir una cifra sin naturaleza y confiar en que alguien la etiquete despues: cifra() LANZA, y no asume ni siquiera la peor, porque asumir convierte un olvido en un dato valido que se propaga · derivar el slug de una tarea del titulo: lo ASIGNA el registro igual que el ID (SUITE-R08). Escribi ocho artefactos en un directorio inventado y lo detecto --gate G2 al no encontrar el intake · atribuir un commit a una tarea buscando el ID en el CUERPO del mensaje: 61 de 162 commits nombran mas de un PT y uno nombra diez, porque citar tareas anteriores es lo CORRECTO en una bitacora append-only. La atribucion es el ASUNTO (PT-057) · usar changes/PT-NNN como medida de esfuerzo: son nueve o diez artefactos SIEMPRE porque es lo que el marco exige en PHASE 4. Mide cumplimiento, no esfuerzo — y es la senal con MEJOR cobertura, asi que es la trampa facil · dar una cifra derivada de menos de MINIMO_REFERENCIA tareas: una mediana de una tarea no es una mediana, y engana por precision aparente · escribir una secuencia de escape unicode en tools/ con una herramienta que la interprete: se convierte en el BYTE crudo, sigue funcionando y es invisible al leer. Lo detecta «audit» · correr un caso del arnes que lea el REGISTRY real sin pasar el ROOT explicito: el arnes trabaja dentro del fixture y el caso mide cuatro tareas de mentira · probar una comprobacion nueva SOLO donde trabaja el agente: PT-056 se puso verde en local y rojo en CI DOS veces —una porque «siguiente» exigia credencial de tablero que en CI no hay, otra porque actions/checkout deja detached HEAD y «rev-parse --abbrev-ref» devuelve la cadena «HEAD»—. El merge se decide en CI, no aqui · medir los tokens restantes del modelo en PT-057: el estimador se construye sobre senales observables e historial real (decision 4 del firmante), y SIN EVALUAR no es presupuesto disponible · exigir que el «sha» del checkpoint sea IGUAL a HEAD: entre dos transiciones de fase hay varios commits —EP-014 hizo hasta diez por tarea contra nueve transiciones— y el aviso saltaria despues de cada uno. Lo que distingue es la HISTORIA, no la igualdad: antecesor de HEAD va por detras y no miente (LEX-R26) · reparar automaticamente un STATE_MISMATCH: reescribir el checkpoint borra la unica prueba de que hubo divergencia, y decidir si manda el arbol o la foto es de SUITE-R06. La herramienta PROPONE «tracker checkpoint PT-NNN» · escribir un caso de selftest que llame a «tracker siguiente» sin que el fixture declare plataforma: la accion se planta y el caso pasa por VACIO. Hay un caso —«tracker siguiente llega a correr»— que existe solo para impedirlo · inventar una cifra de contexto restante porque hace la compuerta de EP-015 mas util: el marco NO PUEDE medirlo y seria un dato falso CON FORMA DE MEDIDA (decision 4). Si el presupuesto derivado no basta para decidir, SE DICE · tratar SIN EVALUAR como cero: un presupuesto sin datos pareceria holgado y el marco arrancaria tareas justo cuando menos sabe · escribir a mano en cauce/<usuario>: es DERIVADA y cada commit lleva la marca «cauce:proyeccion». Uno sin ella se REPORTA, porque una rama derivada en la que alguien escribe deja de serlo · avanzar de fase a mano editando REGISTRY.phase: existe «tracker avanzar PT-NNN --a N --nota», que hace los CINCO actos —registro, YAML, checkpoint, espejo y nota— o no hace ninguno · anadir a CHECKPOINT.json un campo que solo pueda rellenar la memoria (LEX-R26): miente CON LA AUTORIDAD DE UN DATO ESTRUCTURADO, y eso es peor que decirlo en prosa. La especificacion de EP-015 pide «decisions», «blockers» y «estimated_used»: ninguno se deriva de nada · escribir en un comentario de tools/ el patron literal de una emision (la palabra fail o warn seguida del ID entre comillas): fallosPosibles lo cuenta como emision REAL. Hay guarda desde PT-051, pero es una heuristica de linea y no cubre un bloque de comentario en la misma linea que codigo · escribir un caso nuevo que llame a pass/bad directamente en vez de por chk/chkno/trlib/trlibno: se salta el filtro de --solo Y la proteccion de revento(). PT-050 encontro uno asi, y llevaba anos sin proteccion · extraer un bloque de codigo con `sed -n "/texto/,/fin/p"` cuando el texto aparece TAMBIEN en la linea del caso: la asercion casa su propia definicion y pasa aunque el codigo real desaparezca. Van CINCO en una sesion; se extrae por POSICION · rellenar las filas de «## Cierre del lote» con un ID cualquiera para que RE_RESUELTA case: la compuerta se pondria verde sin que nada este resuelto. Es fabricar un verde, y lo señala PT-055 · escribir a mano en cauce/<usuario> cuando exista: es DERIVADA por decision del firmante, y un commit humano ahi la convierte en la segunda fuente que la decision existe para impedir · fabricar una cifra de contexto restante que el marco no puede medir (decision 4): SIN EVALUAR antes que un numero inventado · escribir `if (gate) fail(...)` en verify-fdge: hace inevaluables las tres compuertas anteriores a G4, y hay un caso que lo pone rojo. La compuerta se nombra, y la tabla esta en patrones.mjs (EXIGIBLE_DESDE) · escribir un verificador de FDGE-R22 que compruebe que una declaracion se cumplio: PT-023 lo midio y da 75% de falsos positivos, con cuatro causas ninguna afinable · editar a mano BACKLOG.md ni los tres indices (DISCOVERY, ENRICHMENT, REFACTOR_SCOPE): se DERIVAN del registro. REFACTOR_SCOPE acabo con catorce filas pegadas en una linea por editarlo a mano, y BACKLOG llevo ocho lotes declarando un estado de tres versiones atras · corregir a mano graphify-out/: es regenerable (SUITE-R37) y la correccion se pierde sin avisar · relajar SUITE-R43 para que el mensaje de cierre no cuente: se arregla quien escribe, no la regla que lo detecta · copiar el intake al issue (SUITE-R35) · consolidar varias transiciones en un solo comentario: una nota por transición, al cerrar cada fase · commitear volcados de logs como evidencia sin redactar (FDGE-R45 — ya pasó una vez) · escribir guardas de CI para casos que no se pueden probar desde aquí · fabricar artefactos para poner una compuerta en verde · mergear a main ni publicar sin decisión humana (SUITE-R06, EXEC-R04) · borrar origin/desarrollo sin confirmar que no cuelga nada (SUITE-R06f) · doblar EP-017 dentro de la entrada 9.0.0 del CHANGELOG: esa entrada es el registro fechado de EP-016 y reescribirla borra que lote trajo que — la enfermedad que esta epica combate · dar por cumplido un AC con una medicion PARCIAL: paso en PT-079 con «0 de 17» sobre los issues vivos cuando el tablero completo tenia 20 de 40 rotos · escribir una asercion que exija una salida VACIA con «^$»: una salida vacia no tiene lineas, grep no casa y el caso acusa al codigo. Se serializa y se exige «[]» · declarar plataforma como OPCIONAL mientras «tracker avanzar» la EXIGE: es la contradiccion H7 de PT-072 · fusionar un PR sin ejecutar «gh pr checks»: lo hice con #148 y #149, los dos en rojo. Desde PT-082 la proteccion de rama lo impide, pero el habito era mio · escribir «Termina cuando» sin DOS PUNTOS: FDGE-R53 exige «Termina cuando:» y van tres veces · asignar un ID en el registro y no abrir su issue: el espejo BLOQUEA (SUITE-R35), y paso con PT-083 y PT-084 · declarar plataforma en un proyecto de prueba para que todo fluya: habria ocultado H7, el unico hueco critico de PT-072 · verificar ANTES de commitear y creer que basta: SUITE-R34 compara marcas de COMMIT, asi que solo se pone rojo cuando el commit existe. El orden es commitear, verificar, empujar · empujar directo a «trabajo»: desde PT-082 la proteccion lo rechaza —«protected branch hook declined»— y hay que abrir PR, que es lo que FDGE-R19 ya decia · crear el tag v10.0.0 ANTES de que la version este en la rama por defecto: la linea base del detector apuntaria a un arbol sin lo que la version trae, y callaria sobre sus propias reglas nuevas — el mismo error que PT-081 cometio eligiendo origin/main · escribir PT-073 antes que PT-083 y PT-084: los tres documentos se redactan con lo que la ejecucion enseño · abrir H-010 por la sexta instancia del patron del proxy: es la MISMA forma en una sexta comprobacion y se anade como revision de H-003 (PTSA-R19). Abrirlo aparte diluiria justo lo que el hallazgo afirma — que la causa no esta en las instancias · citar en prosa el verbo de integracion seguido de dos puntos dentro de un intake o de HISTORY.log: RE_QUIEN lo busca en TODO el archivo y SUITE-R27 acusa al texto de ser una firma invalida. Paso TRES veces en una hora, las tres por describir el defecto. Es la sexta instancia del patron y la cierra PT-087 · escribir verificadores para subir 112/224: es fabricar verdes, y esta declarado OUT en EP-018 §3 · empezar EP-018 por PT-087: el mecanismo naceria sin un solo caso real que lo hubiera ejercitado. PT-088 primero, y sus tres comprobaciones son su banco de pruebas · cerrar un hallazgo BUG o DOMAIN de PTSA sin una persona (PTSA-R44) ni sin evidencia post-correccion OBSERVADA en la fuente real (PTSA-R39) · dar por hecho que el score sube al cerrar EP-018: si QA y FPGE encuentran lo que encontro PTSA, BAJA antes de subir, y eso es el lote funcionando · escribir el nombre de una clase de error de node en un comentario de tools/: «revento()» del arnes lo busca EN TODA LA SALIDA y trece casos hacen «cat» de esos archivos. Persegui el fantasma en tres repositorios sinteticos antes de mirar mi propio texto. Es el mismo aviso que ya estaba para las EMISIONES, y nadie los habia relacionado · sintetizar el dato que se quiere comprobar a partir de la lista contra la que se compara: el filtro no puede fallar NUNCA y el criterio queda vacuo, con el caso positivo y el negativo pasando los dos · declarar el limite de una comprobacion sin MEDIRLO: escribi en cinco documentos que una alteracion de igual recuento pasaba, y git la representa como borrado mas alta · borrar con «git branch -D» una rama temporal sin comprobar que su commit no lleva trabajo vivo · usar «file://» con una ruta relativa en el arnes: import() exige absoluta y en Windows la del arnes no lo es. Reviento diez casos y el arnes solo dice «la herramienta revento». Se usa pathToFileURL · asertar contra «[]»: para grep es una CLASE DE CARACTERES y el caso no casa su propia salida. Se serializa a una palabra —VACIO—, igual que PT-085 hizo con «^$» · marcar un estado terminal A MANO en el registro: «tracker avanzar --a <ultima>» lo escribe en las DOS fuentes dentro del acto atomico. Hacerlo a mano deja el YAML atras, y de ahi salieron las seis divergencias que PT-089 arreglo — las cause yo cerrando PT-087 y PT-088 · escribir un caso que compare un campo que el fixture NO DECLARA: la comprobacion no tiene nada que hacer y el caso pasa POR VACIO · un salto de linea dentro de «sed s///»: no es portable. Se usa el comando «i» · versionar graphify-out/: son 2,3 MB medidos, medio de ellos graph.html regenerable, mas un conflicto de merge por regeneracion — y SUITE-R37 lo declara regenerable. Se decidio en PT-090 con la cifra delante · calcular una huella sobre bytes CRUDOS: un checkout con CRLF y otro con LF dan hashes distintos para el mismo archivo, que es el defecto que se estaba cerrando · corregir a mano una cifra del inventario o de CLAUDE.md: caduca en cuanto alguien anada diez lineas. Se recalcula con «tracker inventario --aplicar», y H-006 ya vio caducar ese arreglo una vez · exigir plataforma a una accion que no espeja nada: dejaria sin arreglo al proyecto de una persona que SUITE-R22 declara soportado · atar una asercion a una cifra que crece: lo advertia este mismo bloque y lo hice igual en PT-088 con «PENDIENTE 122». Fallo dos tareas despues, al entrar EXEC-R04a · dar por hecho que una regla NUEVA llega a CORE.md: los dos extractores de build-core rechazaban los sub-IDs que LEX-R24 admite, y la regla contaba como existente sin estar en el nucleo · marcar un lote CLOSED DESPUES de su G4: el estado terminal no llega a la principal con el merge y SUITE-R46 rechaza cerrar su issue, lo que obliga a un merge extra que EXEC-R03 prohibe multiplicar. Lo que convierte un lote en terminal es que su TRABAJO este completo, no el merge: se marca ANTES. EP-017 lo declaro como huevo y gallina inevitable y no lo era · declarar «el repositorio esta limpio» mirando git status y los issues sin mirar el ESTADO DE LOS WORKFLOWS: main llevaba una hora en rojo con cinco errores cuando lo dije. Es la novena instancia del proxy en lugar del hecho, cometida al INFORMAR · reparar un rojo de CI por fuera del marco porque corre prisa: empece moviendo CHECKPOINT.json sin intake, sin PT y sin issue, y lo corto el firmante. SUITE-R41 dice que este repositorio se mantiene bajo su propio marco, y la prisa por desbloquear la publicacion es exactamente cuando eso se salta · aplicar «tracker checkpoint PT-NNN» que el mensaje de STATE_MISMATCH propone sin mirar si el PT esta cerrado: para una tarea terminal NO EXISTE valor correcto que escribir en «rama», y regenerarlo solo mueve el fallo —escrito en main falla en trabajo y al reves · borrar CHECKPOINT.json para poner el verificador en verde: deja verde borrando el dato en vez de corregir la lectura, y el siguiente lote lo reproduce igual · escribir «phase: N» a mano en el registro: FDGE-R52 lo caza contando notas de reanclaje, y volver atras cuesta caminar la escalera entera con «tracker avanzar». Lo hice en PT-094 y fueron ocho transiciones · dar por hecho que «avanzar» pone un BUG en VALIDATION_PENDING: LEXICON dice «IN_REVIEW → VALIDATION_PENDING: tipo BUG · siempre» y el comando NO lo aplica. PT-094 llego a PHASE 9 con status READY y verify-fdge verifico limpio · conectar una guarda nueva a UN solo camino: PT-056 construyo «ramaDeclaradaViva» para el caso exacto que dejo main en rojo y la puso solo en «checkpoint()», no en «avanzar», que es el que de verdad escribe el checkpoint · dar por hecho que un rojo nuevo lo trajo el ultimo cambio: los seis EXEC-R04a ya estaban en 338a728, y comprobarlo costo un minuto y evito escribir un PT sobre una causa inventada · apuntar como verde una prueba inversa que sale EN CERO: paso en PT-095 y tapaba que «corregidaDespues» era una llave maestra que excusaba todo el ledger anterior. Una inversa en cero no es un verde, es un aviso · escribir un bloque de casos ANTES de donde se define el ayudante que usan: «PAT: command not found» sale como salida inesperada, no como «ese ayudante no existe todavia» · extender «alcanzadaPor» a las ocho reglas de RIGE_DESDE porque cabria en una tarde: solo se ha medido que haga falta en EXEC-R04a, y aplicarlo a ciegas es lo mismo que PT-095 corrige · escribir «Estado: DONE» en un BUG sin la firma humana de G3: paso DOS veces hoy —PT-094 con INTEGRATED antes del merge y PT-095 con DONE antes de la validacion— y las dos las paro «--gate G4», no «verify-fdge --all». Las comprobaciones de estado viven en la compuerta · tocar changes/ DESPUES del ultimo commit que toco HANDOFF.md: SUITE-R34 compara marcas de COMMIT y salta en CI, no en local. Van TRES veces en esta sesion. El orden es: el trabajo, luego el estado, y el estado se commitea el ULTIMO
actualizado:    2026-08-21 · PT-095 en DONE con G3 firmada · PR #183 esperando el merge de G4
<!-- /ESTADO -->

---

## `EP-018` — lo que la auditoría encontró   `2026-08-20`

**Abierta y con `G1` resuelta.** Siete tareas en `READY`, ninguna empezada.

| Orden | Tarea | Sev | Qué cierra | Hallazgo |
|:---|:---|:---|:---|:---|
| 1 | `PT-088` #164 | S1 | `SUITE-R09`, `EXEC-R04` y `SUITE-R01` se verifican **o se declaran** | `H-002` |
| 2 | `PT-087` #163 | S1 | Una comprobación declara **qué hecho establece** | `H-003` |
| 3 | `PT-089` #165 | S2 | La divergencia registro/YAML deja de apagar comprobaciones | `H-004` |
| 4 | `PT-090` #166 | S2 | La frescura del grafo es comprobable en cualquier clon | `H-005` · `TD-17` |
| 5 | `PT-091` #167 | S3 | Las cifras del inventario se derivan | `H-007` · `H-006` |
| 6 | `PT-093` #169 | S2 | El límite de las compuertas se declara | `H-009` |
| 7 | `PT-092` #168 | S2 | Ejecutar `QA` y `FPGE` | `H-008` · `TD-15` |

**El orden no es preferencia.** `PT-088` va primero porque sus tres comprobaciones son el **banco
de pruebas** del mecanismo que construye `PT-087`: si el mecanismo no sabe expresar qué mide
`SUITE-R09`, está mal, y se sabe antes de imponerlo a 224 reglas. `PT-092` va último para
ejecutar `QA` y `FPGE` sobre el marco **ya corregido**.

**Dos tareas devuelven la pelota al firmante a mitad**, y está declarado en el intake §8:
`PT-093` pide una decisión de diseño, y `PT-092` tiene que decidir qué es «usar el sistema» para
un paquete de línea de comandos sin navegador.

Auditoría de origen: [`PTSA/RESUMEN.md`](../../PTSA/RESUMEN.md) · certificación **B** ·
el score **caduca el 2026-09-20** (`PTSA-R20`).
