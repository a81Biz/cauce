# `PT-160` · autorrevisión — `PHASE 6` Evidence

## La comprobación encontró **seis criterios que nadie comprobó**

```
6 bloqueantes  el intake declara un AC y la matriz NO lo recoge
13 avisos      la matriz recoge un AC que el intake no declara
```

`PT-077` declara `AC-06` —*«ninguna otra acción que escriba se salta la guarda»*— y su matriz **no
lo recoge**. En verde durante cincuenta tareas.

**Lleva frontera y no rojo.** Los seis son trabajo **integrado**: juzgarlo hacia atrás es `CE-014`,
y el rojo **no tendría salida** porque esas matrices ya se cerraron. Añadirles una fila hoy sería
**inventar una comprobación que nunca ocurrió**.

**Y la obligación tiene ID propio.** Emitir `FDGE-R15` habría metido la frontera a **toda** la
regla, que rige desde siempre. Es la misma decisión que `PT-151` con `SUITE-R01`/`SUITE-R62`: **una
comprobación nueva no puede cambiar el alcance de una regla vieja**.

## Lo medido

Batería: **1795 casos, cero rojos**. El detalle por criterio vive en
`changes/PT-160-*/traceability.md`, que es la matriz canónica (`FDGE-R15a`), y en las
paradas de la tarea. Aquí no se repite: `RULE-01`, un hecho tiene un solo sitio.
