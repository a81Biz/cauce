# `PT-161` · autorrevisión — `PHASE 6` Evidence

## Cuatro puertas del marco no estaban en el catálogo que promete listarlas

```
triggers declarados:     11
sin caso en el catálogo:  4   [CIERRA] · [IMPLEMENTACIÓN] · [FOUNDATION VALIDATED] · [START RECONCILE]
```

**Dos de ellos son el bucle por el que pasa todo el trabajo de `FDGE`** — abrir y cerrar una
implementación.

La promesa entera —«todo caso está aquí»— **no** se puede verificar: nadie sabe qué casos
**existen**. Se comprueba su parte derivable —los puntos de entrada— y **se dice cuál no lo es**.

Los tres casos que faltaban se escribieron **leyendo qué hace cada trigger** —`FDGE-R50`,
`FND-R15`, `PHASE 6` de Foundation—, no inventándolo.

**Y se cerró una deuda de `PT-163`**: `EXEC-R15` no la citaba ningún documento operativo.
Renumerar movió las citas existentes, pero la regla **nunca había estado** en `PHASES`.

## Lo medido

Batería: **1795 casos, cero rojos**. El detalle por criterio vive en
`changes/PT-161-*/traceability.md`, que es la matriz canónica (`FDGE-R15a`), y en las
paradas de la tarea. Aquí no se repite: `RULE-01`, un hecho tiene un solo sitio.
