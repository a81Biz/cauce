migrate — Methodology Suite → 9.0.0
Proyecto: C:\DevOps\Desarrollos\Inteligencia de Mercados Energéticos Mexicanos
Versión detectada: 4.12.0
Modo: informe (añade --apply para ejecutar)

SE HARÍA AUTOMÁTICAMENTE
  · REGISTRY.suite_version: 4.12.0 → 9.0.0

REQUIERE UNA PERSONA
  ! escribir el bloque ESTADO al principio de HANDOFF.md (SUITE-R33). Campos fijos: implementación · tarea · compuerta · siguiente · decisiones · no hacer · actualizado. El formato está en INSTALL.md y en FDGE-Prompts.md. NO lo escribe esta herramienta: declara qué compuerta espera y a quién, y cuál es la siguiente acción — eso no lo sabe una máquina, y rellenarlo con plantilla produce un estado que miente. Sin él, verify-fdge falla. Y se escribe AL CERRAR CADA FASE, no al terminar la sesión: una sesión no siempre avisa de que va a terminar, y SUITE-R34 lo comprueba contra git.
  ! declarar «phase» en 2 allocation(s) viva(s) de REGISTRY.json (EP-009, EP-014). Desde 6.0.0, los artefactos se exigen DESDE la fase que los produce. Y desde 8.0.0 esto DEJA DE SER UN AVISO: un PT vivo sin «phase» falla en verify-fdge (SUITE-R08). Un lote no la declara —su ciclo no tiene fases de tarea— y lo ya terminado tampoco: no se retrofecha.
  ! OPCIONAL — declarar plataforma de trabajo: "tracker": { "plataforma": "github" }. Con ella, el estado deja de vivir en la memoria del agente: cada tarea tiene su issue con su fase y su compuerta, y el espejo se comprueba en las compuertas (SUITE-R35). Sin ella no cambia nada. Declararla es una decisión humana, y activa además SUITE-R42 —G4 sobre un pull request— y SUITE-R43 —los comentarios se leen—.
  ! lo que llega nuevo en tools/: 7 que tu proyecto no tenía — comparar-marco.mjs · patrones.mjs · regla.mjs · revisar-secretos.mjs · tracker.mjs · verify-patrones.mjs · version.mjs. Y los documentos de FIDE/ más INTAKE/templates/TAREA.md.
  ! si el escáner de secretos encuentra un falso positivo, se firma en docs/implementation/SECRETOS-EXCEPCIONES.md — una fila por huella, con nombre y motivo. Firmar no silencia: la excepción sigue apareciendo en cada revisión (FND-R29).
  ! actualizar suite_version a 9.0.0 en el CLAUDE.md del proyecto.

Nada se ha modificado. Ejecuta con --apply.

1 automática(s) · 6 pendiente(s) de decisión humana.

-- que te toca decidir, y por que es tuyo --

Son 6. No es burocracia: cada una es algo que una maquina no puede
saber sin inventarselo, y un estado inventado es peor que un estado ausente.

  1/6 · escribir el bloque ESTADO al principio de HANDOFF.md (SUITE-R33)
        Declara que compuerta esperas y a quien. Rellenarlo con plantilla produce un estado que miente.

  2/6 · declarar «phase» en 2 allocation(s) viva(s) de REGISTRY.json (EP-009, EP-014)
        En que fase esta cada trabajo vivo. La maquina ve los archivos, no la intencion.

  3/6 · OPCIONAL — declarar plataforma de trabajo: "tracker": { "plataforma": "github" }
        Sacar el estado a un tablero es decision de equipo, no tecnica. Sin ella no cambia nada.

  4/6 · lo que llega nuevo en tools/: 7 que tu proyecto no tenía — comparar-marco.mjs ·…
        Herramientas que tu proyecto no tenia. Miralas antes de que aparezcan en una compuerta.

  5/6 · si el escáner de secretos encuentra un falso positivo, se firma en…
        Si una huella es falso positivo lo sabe quien conoce el dato. Firmar no silencia.

  6/6 · actualizar suite_version a 9.0.0 en el CLAUDE.md del proyecto.
        El archivo que parametriza tu proyecto. Solo tu sabes que mas hay ahi.

Ninguna depende de otra: resuelvelas en el orden que quieras. Cuando esten,
vuelve a ejecutar esto y te dira cuales quedan.

Mientras queden pendientes, el proyecto sigue en modo restringido (SUITE-R17):
solo migrate, status * y terminar los PTs ya en vuelo. No se abre trabajo nuevo.
No es un castigo: abrir trabajo sobre una migracion a medias produce artefactos
que luego hay que rehacer, y nadie sabria cuales.
